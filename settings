pw=${1:-password}
host=127.0.0.1
hosturl=http://${host}
clusterport=8091
queryport=8093
axqueryport=8095
cluster=${hosturl}:${clusterport}
queryservice=${hosturl}:${queryport}/query/service
axqueryservice=${hosturl}:${axqueryport}/analytics/service
port=12200
